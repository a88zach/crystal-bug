{
  "name": "crystal-bug",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/crystal-bug/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/crystal-bug"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "crystal-bug:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "crystal-bug:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "crystal-bug:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "crystal-bug:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "crystal-bug:build:development"
        },
        "production": {
          "buildTarget": "crystal-bug:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/crystal-bug"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/crystal-bug/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
